# CI

GitHub Actions runs unit tests and assembles the debug APK for pushes to main and feature branches and for pull requests targeting main.

The resulting APK is uploaded as the `shift-enhanced-debug-apk` workflow artifact.
