# Release process

Every release should pass unit tests and produce an installable debug or release APK through GitHub Actions.

Release notes should state the Android API target, supported device scope and known limitations.
